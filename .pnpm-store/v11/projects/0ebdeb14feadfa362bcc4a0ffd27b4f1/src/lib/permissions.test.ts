import { describe, expect, it } from 'vitest'; import { can } from './permissions';
describe('admin permissions',()=>{it('limits role management to super admins',()=>{expect(can('SUPER_ADMIN','manageRoles')).toBe(true);expect(can('ADMIN','manageRoles')).toBe(false)});it('prevents support from moderating',()=>{expect(can('SUPPORT','moderate')).toBe(false);expect(can('MODERATOR','moderate')).toBe(true)});it('limits settings while allowing every administrator to read',()=>{expect(can('ADMIN','settings')).toBe(true);expect(can('MODERATOR','settings')).toBe(false);expect(can('SUPPORT','read')).toBe(true);expect(can(undefined,'read')).toBe(false)})});
